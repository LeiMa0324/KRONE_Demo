//import { VisualizeTable } from "@/pages/visualize_table";
//import { render, screen } from "@testing-library/react";

describe("VisualizeTable Component", () => {
    it("Placeholder", () => {
        //
    })
    /*
    it("Correctly updates display on Run Option button press", () => {
        render(<VisualizeTable>
        </VisualizeTable>);
    })

    it("Contains file upload button", () => {
        render(<VisualizeTable>
        </VisualizeTable>);

        expect(screen.getByText("Run Option")).toBeInTheDocument();
    }) */
})